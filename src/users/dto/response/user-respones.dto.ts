export interface UserResponse {
  _id: string;
  email: string;
}
// The code you've provided defines a TypeScript interface called UserResponse. This interface is used to define the structure of the response data for a user